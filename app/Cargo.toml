[package]
name = "app"
version = "0.1.0"
edition = "2021"

[dependencies]
yuvutils-rs = { path = "..", features = [] }
image = { path = "../../../RustRoverProjects/image", features = ["avif-native"] }
yuv-sys = "0.3.6"

[dev-dependencies]
criterion = "0.5.1"

[[bench]]
name = "yuv8"
harness = false
